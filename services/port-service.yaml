apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: port-service
  name: port-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: port-service
  template:
    metadata:
      labels:
        app: port-service
    spec:
      nodeSelector:
        node: node3  # Node Selection to Node 3
      containers:
      - name: port-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/port-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 9081
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als
    
# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: port-service
    k8s-app: port-service
  name: port-service
spec:
  ports:
  - name: http
    port: 9081
    protocol: TCP
    targetPort: 9081
  type: ClusterIP
  selector:
    app: port-service
