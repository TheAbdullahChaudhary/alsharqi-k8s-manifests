apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: netsuite-service
  name: netsuite-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netsuite-service
  template:
    metadata:
      labels:
        app: netsuite-service
    spec:
      nodeSelector:
        node: node2
      containers:
      - name: netsuite-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/netsuite-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 9050
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als

# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: netsuite-service
    k8s-app: netsuite-service
  name: netsuite-service
spec:
  ports:
  - name: http
    port: 9050
    protocol: TCP
    targetPort: 9050
  type: ClusterIP
  selector:
    app: netsuite-service
