apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: product-definition-service
  name: product-definition-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-definition-service
  template:
    metadata:
      labels:
        app: product-definition-service
    spec:
      nodeSelector:
        node: node2
      containers:
      - name: product-definition-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/product-definition-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 8543
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als

# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: product-definition-service
    k8s-app: product-definition-service
  name: product-definition-service
spec:
  ports:
  - name: http
    port: 8543
    protocol: TCP
    targetPort: 8543
  type: ClusterIP
  selector:
    app: product-definition-service
