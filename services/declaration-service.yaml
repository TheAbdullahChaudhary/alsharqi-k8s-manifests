apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: declaration-service
  name: declaration-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: declaration-service
  template:
    metadata:
      labels:
        app: declaration-service
    spec:
      nodeSelector:
        node: node2
      containers:
      - name: declaration-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/declaration-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 9212
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als

# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: declaration-service
    k8s-app: declaration-service
  name: declaration-service
spec:
  ports:
  - name: http
    port: 9212
    protocol: TCP
    targetPort: 9212
  type: ClusterIP
  selector:
    app: declaration-service
