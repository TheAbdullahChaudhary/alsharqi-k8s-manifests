apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: document-service
  name: document-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: document-service
  template:
    metadata:
      labels:
        app: document-service
    spec:
      nodeSelector:
        node: node2
      containers:
      - name: document-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/document-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 9087
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als

# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: document-service
    k8s-app: document-service
  name: document-service
spec:
  ports:
  - name: http
    port: 9087
    protocol: TCP
    targetPort: 9087
  type: ClusterIP
  selector:
    app: document-service
