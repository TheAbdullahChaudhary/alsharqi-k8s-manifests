apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: track-n-trace-service
  name: track-n-trace-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: track-n-trace-service
  template:
    metadata:
      labels:
        app: track-n-trace-service
    spec:
      nodeSelector:
        node: node2
      containers:
      - name: track-n-trace-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/track-n-trace-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 9182
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als

# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: track-n-trace-service
    k8s-app: track-n-trace-service
  name: track-n-trace-service
spec:
  ports:
  - name: http
    port: 9182
    protocol: TCP
    targetPort: 9182
  type: ClusterIP
  selector:
    app: track-n-trace-service
