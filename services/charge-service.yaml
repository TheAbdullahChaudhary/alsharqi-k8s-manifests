apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: charge-service
  name: charge-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: charge-service
  template:
    metadata:
      labels:
        app: charge-service
    spec:
      nodeSelector:
        node: node2
      containers:
      - name: charge-service
        image: 218828204349.dkr.ecr.us-east-2.amazonaws.com/charge-service:latest
        imagePullPolicy: Always
        env:
          - name: MY_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: MY_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - containerPort: 9101
          name: http
          protocol: TCP
        volumeMounts:
        - name: log-path
          mountPath: /var/log/als
      volumes:
      - name: log-path
        hostPath:
          path: /var/log/als

# service type ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: charge-service
    k8s-app: charge-service
  name: charge-service
spec:
  ports:
  - name: http
    port: 9101
    protocol: TCP
    targetPort: 9101
  type: ClusterIP
  selector:
    app: charge-service
